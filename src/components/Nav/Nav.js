//this will have to be put into every other component due to routing not working.
import React from "react";

export default function Nav() {
  return (
    <div>
      <hr />
      <button>Home</button>
      <button>New Post</button>
      <button>Logout</button>
      <hr />
    </div>
  );
}
